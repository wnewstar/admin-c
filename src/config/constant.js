export default {
    bitmap: {
        model: {
            bitModal: Math.pow(2, 0),
            bitCreate: Math.pow(2, 1),
            bitAddSon: Math.pow(2, 3),
            bitModify: Math.pow(2, 5),
            bitDelete: Math.pow(2, 7),
            bitRegain: Math.pow(2, 9),
            bitSelect: Math.pow(2, 11),
            bitShowData: Math.pow(2, 12),
            bitShowPage: Math.pow(2, 20),
            bitShowOrder: Math.pow(2, 25),
            bitTreeToList: Math.pow(2, 30)
        }
    }
}
